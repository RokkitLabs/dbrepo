- name: NUnit Reporter
  # You may pin to the exact commit or the version.
  # uses: MirrorNG/nunit-reporter@8abbab7fdac37ef57d1621a5cfa508cdf000428f
  uses: MirrorNG/nunit-reporter@v1.0.5
  with:
    # github token
    access-token: 
    # glob to junit xml files
    path: 
    # max number of failed tests to include
    numFailures: # optional, default is 30
